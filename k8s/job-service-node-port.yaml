apiVersion: v1
kind: Service
metadata:
  name: job-service-node-port
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30083
  selector:
    component: job-service
        